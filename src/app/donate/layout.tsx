export const metadata = {
  title: "Donate Card",
  description: "Donate Page Card",
};

export default function DonateLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
