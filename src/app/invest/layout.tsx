export const metadata = {
  title: "Invest Card",
  description: "Invest Page Card",
};

export default function InvestLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
